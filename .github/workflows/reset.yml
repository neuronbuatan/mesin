name: reset

on:
  workflow_call:
  push:
  workflow_run:
    workflows: [utama]
    types:
      - completed

jobs:
  _null:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 3
      - run: echo "reset completed!"
